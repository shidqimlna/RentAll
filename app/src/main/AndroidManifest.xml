<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentall">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.chat.ChatActivity"></activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.product.EditProductActivity" />
        <activity android:name=".ui.product.NewProductActivity" />
        <activity android:name=".ui.product.DetailProductActivity" />
        <activity android:name=".ui.splash.LandingActivity" />
        <activity android:name=".ui.splash.RegisterActivity" />
        <activity android:name=".ui.account.EditAccountActivity" />
        <activity android:name=".ui.splash.LoginActivity" />
        <activity android:name=".ui.account.UserAccountActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>